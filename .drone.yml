pipeline:
  build:
    image: node:latest
    commands:
      - npm config set registry https://registry.npm.taobao.org
      - npm install nvm -g
      - cd server
      - nvm use v7.1.0
      - npm install
      - npm test
